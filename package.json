{
  "name": "geraldin-bot",
  "version": "2.2.0-beta",
  "description": "Bot para sinalizar parceiros por WhatsApp quando recebem pedido no Geraldo <aiqfome>",
  "bin": "src/main.js",
  "main": "src/main.js",
  "author": "Renan D.",
  "license": "MIT",
  "dependencies": {
    "chalk": "4.1.2",
    "chrome-launcher": "^0.15.1",
    "dotenv": "14.3.2",
    "node-fetch": "2.6.7",
    "npm-run-all": "^4.1.5",
    "ora": "5.4.1",
    "puppeteer-core": "^15.5.0",
    "semver": "^7.3.7",
    "venom-bot": "github:renandecarlo/venom"
  },
  "overrides": {
    "chalk": "4.1.2",
    "dotenv": "14.3.2",
    "node-fetch": "2.6.7",
    "ora": "5.4.1"
  },
  "pkg": {
    "assets": [
      "node_modules/**/*.*",
      ".env.build"
    ],
    "targets": [
      "node16-win"
    ],
    "outputPath": "../dist"
  },
  "scripts": {
    "start": "node .",
    "prebuild": "npm config set PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true && rm -rf ../dist && rm -rf ../geraldin-bot.v%npm_package_version%.zip && rm -rf node_modules/ && npm install",
    "build": "pkg . -C brotli",
    "postbuild": "cp package.json package-lock.json LICENSE .env.ini.example README.md ../dist && mv ../dist/.env.ini.example ../dist/.env.ini && npm run zip",
    "zip": "powershell Compress-Archive ../dist/* ../geraldin-bot.v%npm_package_version%.zip"
  }
}
